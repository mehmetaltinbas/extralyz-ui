1 return statement for every component at the end of the component, with conditional rendering if necessary

don't include 2 components in file, a component file can only consist of a single component without any other code in it (no constants, no other components, no utilities)

instead of hardcoding values, use constants or enums

a constant.ts file can only consists of a single constant, similarily same for util.ts files, .dto.ts files, .response.ts files...
